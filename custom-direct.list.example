# 复制为 custom-direct.list 并填入要强制直连的域名后缀。
# custom-direct.list 已被 .gitignore 排除(可能含企业内网域名)。
#
#   cp custom-direct.list.example custom-direct.list
#   vim custom-direct.list
#   make
#
# 每行一个域名后缀,# 注释,空行忽略。build.sh 自动注入两处:
#   1) rules 顶部        → DOMAIN-SUFFIX,<域名>,DIRECT(强制直连)
#   2) dns.fake-ip-filter → 跳过 fake-ip,做真实解析拿真 IP
# 适合:公网域名却解析到内网 IP 的企业服务(如内部 GitLab)、geosite 覆盖不到的直连需求。

# example.com
# internal.company.com
